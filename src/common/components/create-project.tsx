"use client";
import { supabase } from "../supabaseConfig/supabaseConfig";

export default function CreateProject() {
  async function onClickHandler() {}
  return <button onClick={onClickHandler}>Ajouter un nouveau projet</button>;
}
